[pytest]
# Only the Person-2 environment/physics tests; keeps pytest from walking
# into "Data Science/", "Frontend/" and the run artefacts.
testpaths = thermal-calculator/tests ansys-pipeline/tests backend/tests packages/packages/contracts/tests
addopts = -ra
